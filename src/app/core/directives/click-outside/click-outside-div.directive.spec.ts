import { ClickOutsideDivDirective } from './click-outside-div.directive';

describe('ClickOutsideDivDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideDivDirective();
    expect(directive).toBeTruthy();
  });
});
